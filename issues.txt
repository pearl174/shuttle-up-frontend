sign up validation do after testing
autofill color has to be different in dark mode 
on error in signup or anywhere else, if its a fetch error or something, redirect to a cat 404 or oops something went wrong cat page.
The cats must be pleased and donated sacrifices to.

profile page is not catching shit. for the fetchProfile function. 
for profile pic, add randomizer to default profile pic. 

change profile to a protected route please (after testing is done ig, when u have backend)

SIMILAR ISSUE: (detect logged in user basically)
when you go back from the profile, the profile should open again. not the sign up page 

the local storage should time out at some point, cause the user stays logged in

heatmap is v1. aspect ratio gap issue. look at leetcode fixes.

add streak somewhere. are we adding other stats too? 

routing to protected routes figure out. rn its a generic error on going to profile page without token
but want smth else

is there a better way to error handle? cat memes for errors maybe 
